export default class Menu {
    constructor(className, source, title, path){
        this.className = className;
        this.source = source;
        this.title = title;
        this.path = path;
    }
}